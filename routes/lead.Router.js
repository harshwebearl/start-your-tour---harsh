// const express = require("express");
// const router = express.Router();
// const Lead_Controller = require("../controllers/lead.Controller");
// const lead_Controller = new Lead_Controller();
// // const isAuthAllowed = require("../middleware/isAuthAllowed");
// const adminUserAuth = require("../middleware/admin-user-auth");

// router.post("/", adminUserAuth, (req, res) => lead_Controller.add_leads(req, res));

// router.get("/all", adminUserAuth, (req, res) => lead_Controller.display_all_leads(req, res));

// router.get("/", adminUserAuth, (req, res) => lead_Controller.display_lead(req, res));

// router.put("/", (req, res) => lead_Controller.update_leads(req, res));

// router.put("/allocatedleads", (req, res) => lead_Controller.allocatedleads(req, res));

// router.delete("/delete", adminUserAuth, (req, res) => lead_Controller.delete_lead(req, res));

// module.exports = router;
